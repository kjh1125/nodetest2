<h1>

        
    <!-- static 이용하여 이미지 올리기
    app.use(express.static(path.join(__dirname, 'public'))); 명령어가 지정되어있어야 함.
    public 폴더에 있는 파일은 src로 바로 불러오기 가능.
    -->
    <a href="./index"><img height="50" width="50" src="/images/apple.png">게시판 로고</a> 
</h1>